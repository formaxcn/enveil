## 修改内容

### 1. Sites 标签
- **图标**: `Globe` (地球)
- **标题**: `Site Configurations`

### 2. Clouds 标签
- **图标**: `Cloudy` (多云)
- **标题**: `Cloud Environments`

### 需要修改的文件
[options/App.tsx](file:///d:\Github\enveil\entrypoints\options\App.tsx)

### 具体修改位置
1. **第 3 行**: import 语句 - 将 `Settings` 替换为 `Globe`，添加 `Cloudy`
2. **第 356 行**: Sites 标签 - 替换图标和文字
3. **第 364 行**: Clouds 标签 - 替换图标和文字